spring:
  profiles:
    active: ${springProfile:test}
  application:
    name: lanchonete-api-pagamento
server:
  servlet:
    context-path: /pagamento
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

lanchonete:
  pedido:
    url: http://localhost:8081/pedido